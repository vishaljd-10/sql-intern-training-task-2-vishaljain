# sql-intern-training-task-2-vishaljain
**Short Description:**  This project demonstrates how to create and manage a `students` table in MySQL using XAMPP (phpMyAdmin). It covers using proper data types, applying constraints like PRIMARY KEY, NOT NULL, and UNIQUE, testing constraint errors, and modifying the table structure using ALTER TABLE to ensure data integrity.
